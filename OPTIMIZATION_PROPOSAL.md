# repo-to-resume 优化方案

> 状态：待评审  
> 日期：2026-08-13  
> 对标来源：`C:\Users\wps\Desktop\bian-intern`  
> 本文只描述改造方案，不代表现有 Skill 已完成这些能力。

## 1. 改造目标

将 `repo-to-resume` 从“代码分析后直接生成 STAR 简历”的线性 Skill，升级为覆盖多类求职者的项目经历生产与面试准备系统。

系统需要同时服务以下用户：

- 有真实实习且有明确产出的人；
- 有真实实习但工作零散、缺少亮点的人；
- 没有实习、只有自研项目的人；
- 没有实习、需要从开源项目或他人项目中选择内容进行复现、改造和包装的人；
- 已经有简历，需要补充技术细节、接受压力面试和修复漏洞的人。

目标不是只生成一份好看的简历，而是让用户最终获得一套能经受追问的材料：

1. 项目与业务链路分析；
2. 经历来源和个人角色说明；
3. 经历增强或项目复现任务；
4. STAR 简历；
5. Bullet Point 压力测试报告；
6. 面试分层话术；
7. 学习路径；
8. 模拟面试报告；
9. 最终质量门禁报告。

## 2. 核心流程

```text
用户状态识别
  -> 项目/经历来源判断
  -> 代码事实与业务链路分析
  -> 用户角色与掌握度确认
  -> 经历增强 / 项目复现 / 方案补全
  -> STAR 简历包装
  -> Bullet Point 逐条拷打
  -> 学习补漏与面试话术
  -> 模拟面试
  -> 跨产物质量门禁
```

每一步都允许用户从中间进入。主 Skill 不应强制所有用户走完整流程，而应根据现有材料自动路由。

## 3. 用户模式

### 3.1 `REAL_INTERNSHIP`

适用条件：用户有真实实习，并且能够说明自己参与的模块或任务。

处理方式：

- 从代码和用户口述中提取业务链路；
- 区分团队成果与个人成果；
- 把零散工作串成端到端故事；
- 对已有成果做工程深挖、表达优化和数据补充；
- 生成实习经历和面试材料。

### 3.2 `WEAK_INTERNSHIP`

适用条件：用户有真实实习，但实际工作偏杂、偏少，或者无法形成完整项目故事。

处理方式：

- 扫描公司或实习期间接触的项目；
- 找到用户接触过、能够学习和复现的核心链路；
- 结合真实工作内容补充业务背景、技术上下文和方案演进；
- 为缺少的监控、容错、测试、性能验证等环节设计补全方案；
- 通过拷打确认用户是否真正掌握包装后的内容。

### 3.3 `SELF_PROJECT`

适用条件：用户没有实习，但有自研、课程或比赛项目。

处理方式：

- 用企业项目视角重新分析项目；
- 补充业务链路、工程约束和技术选型理由；
- 设计可真正执行的增强任务；
- 将项目包装为独立项目经历；
- 准备 30 秒、2 分钟和技术深挖三层话术。

### 3.4 `PROJECT_MIGRATION`

适用条件：用户没有可用经历，需要从开源项目、团队项目或他人项目中选择内容进行学习、迁移和包装。

处理方式：

- 选择与目标岗位匹配的项目和 1-3 条核心链路；
- 要求用户完成代码阅读、运行、关键模块复现或二次开发；
- 为用户建立“必须掌握的代码位置、设计决策、异常场景、验证结果”清单；
- 可将成熟内容用于项目经历或实习叙事，但必须经过掌握度门禁；
- 对未亲自实施的部分标记来源风险，并在拷打中提高追问强度。

### 3.5 `EXISTING_RESUME`

适用条件：用户已经有简历或项目描述，不需要重新扫描完整项目。

处理方式：

- 逐条拆解 Bullet Point；
- 映射到代码、业务链路和个人角色；
- 运行事实、技术、数据、边界和角色五类追问；
- 输出漏洞清单、修复任务和面试话术。

## 4. 路由规则

主 Skill 启动时先判断用户当前状态：

```text
用户是否已有简历或包装文本？
  |- 是 -> EXISTING_RESUME
  `- 否
      用户是否有真实实习？
        |- 是
        |   是否有明确个人产出？
        |     |- 是 -> REAL_INTERNSHIP
        |     `- 否 -> WEAK_INTERNSHIP
        `- 否
            是否有自己完成的项目？
              |- 是 -> SELF_PROJECT
              `- 否 -> PROJECT_MIGRATION
```

如果用户只想理解代码，进入 `UNDERSTAND_ONLY`，只执行分析和学习路径，不生成简历。

## 5. 业务链路分析升级

现有 `references/code-analysis-engine.md` 从产品、模块、数据流、选型、模式和坑点六个维度分析项目。建议保留这些维度，并新增“业务链路与包装价值”层。

### 5.1 每条链路的统一结构

```text
用户或系统入口
  -> 接口 / 命令 / 事件
  -> 核心业务逻辑
  -> 数据库 / 缓存 / MQ / 外部系统
  -> 最终输出或业务消费者
```

### 5.2 技术栈适配

| 技术栈 | 推荐追踪路径 |
|---|---|
| Spring Boot | Controller -> Service -> Repository/MQ/Client |
| Node.js | Route -> Handler -> Domain/Service -> Adapter |
| Go | Router -> Handler -> Use Case -> Repository |
| Python Web | Endpoint -> Application -> Data/External |
| CLI | Command -> Orchestrator -> Core -> IO Adapter |
| 前端 | User Action -> State -> Request -> Render |
| 数据/ML | Data Input -> Transform -> Model/Rule -> Evaluation/Output |

### 5.3 新增链路字段

每条链路必须输出：

- 链路名称；
- 业务域类型：`BUSINESS`、`PLATFORM`、`TOOLING`；
- 输入、处理步骤和最终消费者；
- 关键代码位置；
- 依赖的数据库、缓存、MQ、RPC 或第三方系统；
- 闭环程度；
- 工程复杂度；
- 当前已具备的稳定性能力；
- 可以补充的工程能力；
- 用户在链路上的位置；
- 包装价值和推荐优先级；
- 推荐面试追问；
- 推荐增强实验。

## 6. 角色与来源模型

不再用简单的“真实/不真实”二分法，而是为每条声明记录来源、掌握度和风险。

### 6.1 来源类型

| 类型 | 含义 | 默认处理 |
|---|---|---|
| `PERSONAL_FACT` | 用户亲自完成 | 可直接包装 |
| `TEAM_FACT` | 团队成果，用户参与其中 | 明确个人负责部分 |
| `MENTOR_DIRECTED` | 方案由 mentor 决定，用户负责实现 | 强调实现和理解，不夸大决策权 |
| `CODE_INFERRED` | 只能从仓库确认，用户角色未知 | 进入角色确认 |
| `SELF_PROJECT` | 用户自研项目成果 | 作为项目经历包装 |
| `REPRODUCED` | 用户复现或二次开发的内容 | 记录复现范围和新增改动 |
| `BORROWED_CONTEXT` | 来自他人或团队，用户尚未亲自实现 | 可进入学习和迁移流程，高风险 |
| `DESIGNED_ONLY` | 完成方案设计但未上线 | 可写设计与验证，不默认写线上效果 |
| `NARRATIVE_FILL` | 为补全故事而设计的合理工程环节 | 必须进入重点拷打和掌握度检查 |

### 6.2 掌握度等级

| 等级 | 用户能力表现 |
|---|---|
| `L0` | 只听说过，无法解释代码和原理 |
| `L1` | 能说明用途和基本原理 |
| `L2` | 能定位关键代码并解释主流程 |
| `L3` | 能解释方案选择、替代方案和边界 |
| `L4` | 能独立实现、测试、排错并复盘 |

简历核心 Bullet 原则上要求达到 `L3`；高风险来源要求达到 `L4` 或完成指定增强任务。

### 6.3 风险等级

| 风险 | 判定 |
|---|---|
| `LOW` | 个人事实且有代码或用户材料支持 |
| `MEDIUM` | 团队成果或缺少完整数据，但用户能解释 |
| `HIGH` | 他人产出迁移、未执行的工程环节或角色归属不清 |
| `BLOCKED` | 用户无法解释核心实现，且没有完成补强任务 |

风险不是自动禁止条件，而是决定后续需要多强的学习、复现和拷打。

## 7. 经历增强实验室

新增 `references/experience-lab.md`，把项目缺失能力转成用户可执行或可完整推演的工程任务。

### 7.1 支持的增强方向

- 监控、日志和链路追踪；
- 超时、重试、熔断和降级；
- 缓存与一致性；
- 消息可靠性和重复消费；
- 幂等、补偿和对账；
- 灰度、回滚和配置开关；
- 压力测试和性能分析；
- 自动化测试；
- CI/CD；
- 权限和安全；
- 数据质量与异常检测。

### 7.2 增强任务模板

```markdown
## 增强任务：为订单链路补充可观测性

### 当前问题
- 缺少接口延迟、错误率和调用量指标；
- 出现故障时无法快速定位具体阶段。

### 最低完成标准
- 定义 3 个核心指标；
- 设计 2 条告警规则；
- 构造 1 个故障场景；
- 说明阈值选择依据；
- 能解释告警后的排查路径。

### 可选实作
- 接入 Prometheus/Micrometer；
- 添加 Grafana Dashboard；
- 保存配置、测试命令和结果。

### 面试门禁
- 为什么选择这些指标？
- 告警阈值如何避免误报？
- 如果监控系统本身不可用怎么办？
```

### 7.3 实作与方案推演

增强任务分两种完成模式：

- `IMPLEMENTED`：用户实际修改代码并完成验证；
- `DESIGNED`：用户完成完整方案设计、关键伪代码、验证计划和追问准备，但未在真实环境上线。

两种模式都可进入包装流程，但生成文案、风险标记和拷打强度不同。

## 8. 简历生成升级

修改 `references/star-resume-generator.md`，从“统一 STAR 模板”升级为“来源感知的 STAR 生成器”。

### 8.1 输出策略

#### 真实实习

```text
参与订单状态流转模块建设，负责幂等校验与异常补偿逻辑，
减少重复回调导致的状态冲突。
```

#### 弱实习补全

```text
围绕订单处理链路梳理异常场景，补充超时、重试与告警方案，
完善从请求处理到异常恢复的闭环设计。
```

#### 自研项目

```text
设计并实现订单状态机，将分散的状态判断集中管理，
覆盖支付、取消与超时关闭等核心路径。
```

#### 开源复现或项目迁移

```text
基于开源任务调度项目复现核心执行链路，并扩展资源组并发控制，
完成不同并发策略的压力测试与结果对比。
```

#### 方案补全

```text
针对消息重复消费问题设计幂等与补偿方案，
对比 Redis 去重和数据库唯一约束的适用边界。
```

### 8.2 动词规则

废除“参与、协助一律属于弱动词”的规则。动词必须与用户角色和掌握度匹配：

| 角色 | 可用动词 |
|---|---|
| 方案主导 | 设计、主导、推动、制定 |
| 独立实现 | 实现、重构、改造、建立 |
| 团队协作 | 参与、协同、配合、负责其中 |
| 复现迁移 | 复现、迁移、扩展、验证 |
| 方案推演 | 设计、对比、评估、规划 |
| 阅读学习 | 梳理、分析、定位、总结 |

### 8.3 数字规则

不再要求每条 Action 都有量化百分比。数字分为：

- 真实业务数据；
- 本地或测试环境结果；
- 代码静态统计；
- 合理估算；
- 叙事补全数据。

每个数字在内部 `evidence-manifest.json` 中记录来源和风险。没有可靠数字时，可以使用覆盖范围、链路闭环、故障类型、模块数量或验证结果表达价值。

## 9. Bullet Point 拷打门禁

新增 `references/claim-grill.md`。简历生成后，不直接进入学习路径，而是先逐条验证每个 Bullet。

### 9.1 六层追问

```text
事实确认
  -> 个人角色
  -> 技术实现
  -> 方案对比
  -> 数据与验证
  -> 边界与复盘
```

示例：

1. 你说你完成了 X，具体负责哪一部分？
2. 入口方法、核心类和关键数据结构分别是什么？
3. 为什么选择 A，而不是 B？
4. 这个数字来自哪里，如何测量？
5. 依赖服务异常、重复请求或并发提高后会怎样？
6. 如果重新做一次，你会修改什么？

### 9.2 漏洞等级

| 等级 | 含义 | 处理动作 |
|---|---|---|
| `RED` | 一问就穿，无法解释核心实现或角色 | 删除、降级或进入增强实验室 |
| `ORANGE` | 能讲概念，缺少代码和操作体感 | 完成复现或关键修改 |
| `YELLOW` | 做过或掌握主流程，但边界、数据不足 | 补测试、数据和失败场景 |
| `GREEN` | 能从业务、实现、验证和取舍四层展开 | 进入话术与模拟面试 |

### 9.3 高风险来源规则

`BORROWED_CONTEXT`、`NARRATIVE_FILL` 和仅设计未实施的内容，至少需要：

- 用户能够定位关键代码；
- 用户能够画出端到端链路；
- 用户能够解释两个备选方案；
- 用户能够回答两个失败场景；
- 用户能够解释验证方法；
- 连续两轮拷打不存在 `RED`。

## 10. 面试话术生成

新增 `references/interview-script-generator.md`，但不强制生成固定、冗长、需要逐字背诵的稿件。

每条通过拷打的 Bullet 生成四层材料：

### 10.1 15 秒版本

一句话说明业务问题、个人动作和结果。

### 10.2 30 秒版本

使用“背景 -> 动作 -> 结果”结构，适合作为面试官首次询问时的回答。

### 10.3 2 分钟版本

完整说明：

- 问题背景；
- 用户角色；
- 初始方案；
- 遇到的问题；
- 最终方案；
- 方案取舍；
- 验证或结果；
- 当前不足。

### 10.4 深挖材料

- 关键代码位置；
- 数据结构和核心流程；
- 备选方案对比；
- 故障与边界；
- 可继续优化的方向；
- 不能说错的事实；
- 面试官可能继续追问的问题。

## 11. 风险驱动学习路径

现有学习路径按模块重要度排序。优化后增加第二套优先级：

```text
学习优先级 = 简历声明风险 x 面试出现概率 x 用户掌握缺口
```

优先学习：

1. 已写进简历但被标为 `RED/ORANGE` 的内容；
2. 高频追问且用户不能解释的核心链路；
3. 高风险来源对应的实现细节；
4. 方案对比和失败模式；
5. 低风险的扩展知识。

学习任务必须有可验证的达标标准，例如：

```text
能脱离文档画出链路；
能指出 3 个关键文件；
能解释 A/B 方案差异；
能回答 2 个故障场景；
能在 2 分钟内讲完整个技术决策。
```

## 12. 最终质量门禁

新增 `references/quality-gate.md`，在所有材料交付前做跨产物检查。

### 12.1 检查项

- 简历声明是否能映射到代码、用户材料或增强任务；
- 项目整体成果与个人贡献是否在内部记录中一致；
- 数字是否有来源和风险标记；
- 项目迁移内容是否达到规定掌握度；
- 方案设计和实际实施是否使用匹配的表达；
- 面试话术是否超出简历声明；
- 每条核心 Bullet 是否通过拷打；
- 用户是否能定位关键代码；
- 用户是否能解释替代方案和失败模式；
- 学习路径是否覆盖所有高风险项；
- 是否泄露密钥、内部地址或公司敏感信息。

### 12.2 最终状态

| 状态 | 含义 |
|---|---|
| `BLOCKED` | 核心声明无法解释，不能继续增强叙事 |
| `TRAINING_REQUIRED` | 需要先完成复现、学习或测试任务 |
| `REVIEW_REQUIRED` | 内容基本成立，仍有数字或边界风险 |
| `INTERVIEW_READY` | 核心 Bullet 全部通过拷打，可进入真实面试 |

## 13. 模块重构

### 13.1 主 Skill 的职责

主 `SKILL.md` 只保留：

- 用户模式路由；
- 全局状态机；
- 检查点；
- 子模块交接契约；
- 风险处理规则；
- 产物目录规则。

大量输出模板和具体生成规则移入 `references/`，减少主文件重复和维护成本。

### 13.2 建议目录

```text
repo-to-resume/
|- SKILL.md
|- references/
|  |- code-analysis-engine.md
|  |- business-chain-extractor.md
|  |- contribution-mapper.md
|  |- experience-lab.md
|  |- claim-policy.md
|  |- star-resume-generator.md
|  |- claim-grill.md
|  |- learning-path-generator.md
|  |- interactive-learning-template.md
|  |- mock-interviewer.md
|  |- interview-script-generator.md
|  |- quality-gate.md
|  `- output-contracts.md
`- evals/
   |- fixtures/
   |- prompts/
   `- expected/
```

## 14. 模块交接契约

新增 `evidence-manifest.json` 作为所有模块共享的结构化数据源。

```json
{
  "schema_version": "1.0",
  "mode": "PROJECT_MIGRATION",
  "project": {},
  "business_chains": [],
  "contributions": [],
  "enhancement_tasks": [],
  "claims": [],
  "evidence": [],
  "grill_results": [],
  "learning_gaps": [],
  "risk_flags": [],
  "quality_status": "TRAINING_REQUIRED"
}
```

### 14.1 声明对象示例

```json
{
  "id": "CLAIM-003",
  "text": "设计订单消息幂等与异常补偿机制",
  "source_type": "NARRATIVE_FILL",
  "contribution_id": "CONTRIBUTION-002",
  "chain_id": "CHAIN-001",
  "evidence_ids": ["CODE-014", "DESIGN-003"],
  "mastery_level": "L3",
  "risk": "HIGH",
  "status": "NEEDS_TRAINING",
  "grill_status": "YELLOW"
}
```

每个子模块只读写自己拥有的字段，避免简历生成器、学习路径和面试模块各自重新解释事实。

## 15. 检查点设计

### Checkpoint A：模式确认

展示系统判定的用户模式、已有材料和推荐路径，用户确认后继续。

### Checkpoint B：分析范围确认

展示扫描范围、业务链路和跳过目录，防止大型项目无边界扫描。

### Checkpoint C：角色与来源确认

展示每条候选经历的来源、用户角色、掌握度和风险。用户可以修改。

### Checkpoint D：增强任务确认

展示需要实作、复现或方案推演的内容，用户选择任务深度。

### Checkpoint E：简历确认

展示简历和每条 Bullet 的来源映射，不自动进入下一步。

### Checkpoint F：拷打门禁

存在 `RED` 时停止自动推进，先修复或降级声明。

### Checkpoint G：最终质量确认

展示质量状态、剩余风险和推荐用途，由用户决定继续训练还是导出最终材料。

## 16. 输出目录

保持所有产物位于被分析项目内部，但扩充目录结构：

```text
{project-name}-analysis/
|- project-analysis.md
|- business-chains.md
|- contribution-map.md
|- evidence-manifest.json
|- enhancement-plan.md
|- resume.md
|- claim-grill-report.md
|- learning-path.md
|- learning-interactive.html
|- interview-scripts.md
|- interview-report.md
`- quality-gate-report.md
```

继续沿用“不覆盖已有文件”的版本规则。

例外：`evidence-manifest.json` 是固定名称的当前状态文件。更新前将旧版归档到 `history/`，新内容通过 Schema 校验后原子替换；其他产物继续使用 `-vN` 版本规则。

## 17. 评测方案

在实际修改 Skill 前，先建立至少五类测试场景：

| 场景 | 预期能力 |
|---|---|
| 有真实 Spring Boot 实习项目 | 正确识别个人角色并生成实习经历 |
| 有实习但只做零散 CRUD | 串联业务链路并给出增强任务 |
| 无实习、有自研项目 | 生成企业化项目经历，不强行套公司背景 |
| 无实习、使用开源项目 | 输出复现与迁移路径，并运行高强度拷打 |
| 已有夸大的简历 | 找出角色、数字和实现漏洞，给出降级或补强方案 |

每类场景检查：

- 路由是否正确；
- 是否生成业务链路；
- 是否区分来源和角色；
- 是否产生可执行的增强任务；
- 简历是否与模式匹配；
- 高风险内容是否进入强化拷打；
- 最终状态是否合理；
- 各产物是否保持一致。

## 18. 实施顺序

### 第一阶段：建立可信状态模型

1. 增加用户模式路由；
2. 增加来源、角色、掌握度和风险模型；
3. 定义 `evidence-manifest.json`；
4. 增加角色确认检查点；
5. 修改简历动词与数字规则。

交付标准：系统可以区分五类用户，并为候选 Bullet 标记来源和风险。

### 第二阶段：补齐经历生产能力

1. 增加业务链路提取；
2. 增加经历增强实验室；
3. 支持开源/他人项目的复现和迁移；
4. 让简历生成器适配不同来源模式。

交付标准：没有优质实习的用户也能获得明确、可执行的经历构建路径。

### 第三阶段：建立抗追问闭环

1. 增加 Bullet Point 拷打；
2. 增加分层面试话术；
3. 将学习路径改为风险驱动；
4. 让模拟面试读取拷打结果和掌握度。

交付标准：每条核心 Bullet 都有漏洞等级、补强任务和深挖材料。

### 第四阶段：质量与回归

1. 增加最终质量门禁；
2. 建立五类评测 fixture；
3. 检查跨产物一致性；
4. 精简主 `SKILL.md`；
5. 完成回归测试和文档整理。

交付标准：所有用户模式都能完成完整流程，且旧的代码分析、学习页面和模拟面试能力不退化。

## 19. 第一轮建议改动范围

第一轮不一次性重写所有模块，建议只完成以下五项：

1. 用户模式路由；
2. 角色与来源模型；
3. `evidence-manifest.json` 数据契约；
4. 经历增强实验室；
5. Bullet Point 拷打门禁。

暂不修改：

- 交互式学习页面的 UI 模板；
- 模拟面试现有五维评分；
- 输出目录的版本管理机制；
- 现有多语言代码分析能力；
- 现有 STAR 与学习路径的基本展示结构。

这样可以先验证核心价值：系统能否让不同背景的用户得到更可用、更能抗追问的项目经历，再决定是否进行更大规模的模块重构。

## 20. 待评审问题

开始实施前，需要确认以下产品决策：

1. `PROJECT_MIGRATION` 是否允许直接输出“实习叙事”，还是默认先输出项目经历，再由用户选择转换？
2. `DESIGNED` 模式是否允许使用“搭建、建立”等实施型动词？
3. 高风险来源达到 `L3` 是否足够，还是必须达到 `L4`？
4. 是否要求用户实际提交代码修改，还是完整方案推演也可以通过门禁？
5. 数字估算是否在正文直接使用，还是只保存在内部材料中供用户选择？
6. 最终质量门禁是硬阻断，还是只提示风险并允许用户继续？
7. 第一轮实现是否保留现有 `Step 2.5` 交互式学习页面？

这些问题会直接影响主流程的门禁强度、生成措辞和实现复杂度，应在动手修改 Skill 前确定。
